#!/usr/bin/env python3
"""Run displaywright straight from a checkout, no install step needed."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from displaywright.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
